describe('TESTANDO ALUMA COISA', () => {
  it('descrição do teste (IT)', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTANDO OUTRA COISA', () => {
  test('descrição do teste (TEST)', () => {
    const name = 'Gabriel';
    expect(name).toBe('Gabriel');
  });
});
